# Project Instructions

This is a zero-dependency Node.js ES module project.

- Represent money as integer cents.
- Keep public functions deterministic and free of I/O.
- Use the built-in `node:test` runner.
- Do not add dependencies, network access, taxes, currencies, or persistence.
- Run `node --test` for verification.
